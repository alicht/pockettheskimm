<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
  <script>
    $(document).ready(function() {
      $("#skimm").click(function() {
        var opts = {
          lines: 13 // The number of lines to draw
        , length: 28 // The length of each line
        , width: 14 // The line thickness
        , radius: 42 // The radius of the inner circle
        , scale: 1 // Scales overall size of the spinner
        , corners: 1 // Corner roundness (0..1)
        , color: '#000' // #rgb or #rrggbb or array of colors
        , opacity: 0.25 // Opacity of the lines
        , rotate: 0 // The rotation offset
        , direction: 1 // 1: clockwise, -1: counterclockwise
        , speed: 1 // Rounds per second
        , trail: 60 // Afterglow percentage
        , fps: 20 // Frames per second when using setTimeout() as a fallback for CSS
        , zIndex: 2e9 // The z-index (defaults to 2000000000)
        , className: 'spinner' // The CSS class to assign to the spinner
        , top: '50%' // Top position relative to parent
        , left: '50%' // Left position relative to parent
        , shadow: false // Whether to render a shadow
        , hwaccel: false // Whether to use hardware acceleration
        , position: 'absolute' // Element positioning
        }
        var target = document.getElementById('spinner')
        var spinner = new Spinner(opts).spin(target);
      })
    })
  </script>
</head>

<body>
      <% if notice.present? %>
        <div class="alert alert-info">
        <div class="text-center"><%= notice %></div>
          <% end %>
        </div>
<div class="center-block text-center"> 
  <h1>Welcome to Pocket theSkimm!</h1>
  <br>
      <h2>Step 1: <%= link_to 'Sign in with Pocket', '/auth/pocket/connect'%></h2>
      <h2>Step 2: <%= link_to "click here to Pocket all of the articles in today's Skimm", add_path, id: "skimm" %></h2>
      
    <h4>* Note: a new Skimm becomes available every morning at 6am EST *</h4>
    <!-- <h2><a href="http://www.theskimm.com/recent" target="_blank">Today's Skimm</a></h2> -->

    <div id="spinner"> </div>
    <!-- <img src="/assets/pocketskimm.png"> -->
    <%= image_tag "pocketskimm.png" %>
  </div>
  

<!%= image_tag "/assets/pocketskimm.png" %>
</body>
</html>


